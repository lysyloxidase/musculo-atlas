# Dimensions

All hierarchy nodes carry a characteristic `scale_meters` value and a source
citation string. The canonical numeric table lives in
`apps/web/src/lib/dimensions.ts`.

The scale ladder spans whole-body anatomy through atomic-resolution structural
biology references.
